# xhm9 开发日志

> 维护者：xhm9（AI 助手协作）
> 规则：**任务开始、结束时更新本日志**（用户 2026-08-17 立规）
> 团队规则见仓库 README.md 与 docs/团队开发方案.md

---

## 2026-08-18 · 任务：歌词展示改造 v2 移植（进行中）

### 背景
- 琥珀重建分支：**`v2-rebuild` 为新主开发线**（上游 lx-music-mobile 最新 16f5a49 + 团队共享文档）；旧 `main` 为经验存档（旧版自研 Stats/AI/歌词，可参考不可改）；`dev` 停用。
- 之前在旧 main 上完成的歌词改造（PR #2）需移植到 v2-rebuild。

### 移植完成（第一里程碑）
- **commit `f7c1af8`**（分支 `feature/lyric-v2-port`）：13 files changed，+646 / -1137（净减 491 行）
- 移植内容：
  - `LyricView.tsx` 统一歌词视图（滚动 / 点按 seek / 双指缩放 / PlayLine）
  - 三个播放页 Lyric.tsx 薄封装
  - Apple Music 风格：当前行放大 1.15x + 高亮；非当前行透视倾斜（相邻 5° / 更远 9°，上方正 / 下方负）+ BlurView 景深模糊（12/20）+ 透明度递减（0.85/0.7）
  - 设置项「歌词视图模糊」`playDetail.style.lyricBlur`（默认开，仅 Android 12+ 真模糊），入口播放页设置弹窗，三语文案
  - 引入 `@react-native-community/blur@4.4.1`
- 验证：`npx tsc --noEmit` 歌词模块零错误；v2 上游基线 277 条（仅 `LandscapeImmersionSettingPopup` 4 条上游存量错误与本次无关）

### 当前状态
- ✅ **PR #4 已创建**：`https://github.com/AsrnkHoper/lx-lxwalnut-music-mobile/pull/4`（base `v2-rebuild` ← xhm9 `feature/lyric-v2-port`）
- ⏳ 等 review + CI；APK 出包：fork 的 Actions（push 时已触发，feature/** 分支构建）下载验证
- 真机验证重点：模糊强度（12/20）/ 倾斜角度（5°/9°）/ 设置开关效果
- 后续可选：逐音节卡拉OK（AMLL-Lyric）、场景化配色

### 关键备注
- PR 目标：**`v2-rebuild`**（不是 main/dev）
- 旧 main 上的歌词 PR 仅作经验参考，不直接合并
- v2 上游基线 tsc 错误 277 条（比旧版 284 少 7 条，上游更干净）

### CI 修复记录
- **commit `f83e87a`**：v2 上游无 `debug-build.yml`，从旧 main 移植并适配（触发加 `v2-rebuild`，去掉 AiConfig 诊断）
- **commit `60eb553`**：v2 上游 `postinstall` 引用了未提交的 `scripts/apply-rntp-eq-patch.js` / `apply-slider-patch.js`，CI `npm ci` 因此 MODULE_NOT_FOUND 失败；改为只跑存在的 `patchMediaLayout.js`。⚠️ 风险：EQ/slider 补丁缺失，需后续从上游补或团队自研
