# xhm9 开发日志

> 维护者：xhm9（AI 助手协作）
> 规则：**任务开始、结束时更新本日志**（用户 2026-08-17 立规）
> 团队规则见仓库 README.md 与 docs/hoper/团队开发方案.md

---

## 2026-08-17 · 任务开始：歌词展示改造

### 任务来源
- 用户（xhm9）选定负责方向：**歌词展示改造**（产品愿景「实用线」：让歌词不只是文字，是有生命的——逐行/双语/场景）

### 改造方案（用户拍板：先重构 + Apple Music 风格视觉）
1. **重构**：新建 `src/screens/PlayDetail/components/LyricView.tsx` 统一歌词滚动视图（消除 Vertical/Horizontal/LandscapeImmersion 三个 Lyric.tsx 的重复代码）
   - 配置化 props：fontSizeKey / enableZoom / showPlayLine / viewPosition / 颜色 / 对齐 / 行高比
   - Vertical/Horizontal/LandscapeImmersion 的 Lyric.tsx 变薄壳
2. **Apple Music 风格视觉**（融入 LyricView）：
   - 当前行字号放大（1.15x，Animated 平滑过渡）+ 高亮色 + 全不透明
   - 非当前行弱化色 + 按与当前行距离递减透明度（相邻 0.75 / 更远 0.55）
   - 翻译行小字号（0.75x）跟随原文下方，排版对齐
   - 保留现有交互：双指缩放字号、点按 seek、用户拖动 3 秒后恢复自动滚动

### 完成记录（第一里程碑：重构 + Apple Music 风格）
- **commit `5d93f99`**（分支 `feature/lyric-refactor`）：4 files changed，+557 / -1070（净减 513 行）
- 新建 `LyricView.tsx`：统一滚动 / 点按 seek / 双指缩放 / PlayLine / 当前行放大动画
- Vertical/Horizontal/LandscapeImmersion 三个 Lyric.tsx 变薄封装
- Apple Music 风格：当前行放大 1.15x（Animated 800ms）+ 高亮色；非当前行按距离透明度 0.75/0.55；翻译行 0.8x
- tsc：**歌词模块 0 错误**；项目总错误 288 → 284（顺带修复 Landscape 存量错误：非法 `lrcAlign` key、`playedLines` 解构）

### 当前状态
- ✅ 第一里程碑完成，待真机验证视觉效果（需琥珀/手机）
- 后续可选：逐音节卡拉OK（AMLL-Lyric）、场景化配色

---

## 2026-08-17 · 任务开始：实现统计模块 5 个 bug 修复

### 任务来源
- 琥珀《docs/hoper/改动记录_20260817.md》：用户反馈 5 个问题，修复方案已写文档，但**代码尚未上传仓库**。

### 任务范围（按改动记录实现）
| # | 文件 | 修改 |
|---|------|------|
| 1 | `src/core/player/tempPlayList.ts` | 新增 `addTempPlayListAndPlay`（入队并立即切歌） |
| 2 | `src/screens/Home/Views/Stats/MonthHeatMap.tsx` | 账本点歌改用新方法 |
| 3 | `src/screens/Home/Views/Stats/index.tsx` | 排行点歌找回完整 musicInfo；导入强制内置文件浏览器；弹窗先关再开选择器 |
| 4 | `src/components/common/ChoosePath/index.tsx` | `ReadOptions` 新增 `forceInternal` |
| 5 | `src/screens/Home/Views/Stats/ReportView.tsx` | 档案删除确认先关弹窗；导入强制内置浏览器 |

### 开始前准备（已完成）
- 仓库：`AsrnkHoper/lx-lxwalnut-music-mobile` main @ `6ab279f`（含 CODEOWNERS + 团队 README）
- 本地环境：Node v24；`npm install --ignore-scripts` 成功（893 包）
- tsc 基线：**252 条存量错误**；本地实测 262（差异来自 3 个 github 依赖无 lib，用临时 shim 校验），**Stats 模块 0 错误**
- 分支：`feature/stats-bugfix-20260817`（从 main `6ab279f` 分出，工作区干净）

### 当前状态
- ❌ **已取消/移交**：用户确认这 5 个 bug 修复属**琥珀负责的部分**，本分支不再实现
- `feature/stats-bugfix-20260817` 分支已删除，工作区切回 `main`
- 下一步：等待琥珀 push 代码，或用户指派 xhm9 负责的任务

### 关键风险 / 备注
- 琥珀的修复代码未 push；若后续琥珀传了代码，需与本分支实现做对比合并
- 远端暂无 `dev` 分支（团队方案待办之一）
- 本机无 GitHub 推送凭证，push/PR 环节需用户提供方式
